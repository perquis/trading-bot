import { blockers } from "@/services/blockers.service";
import { orders } from "@/services/orders.service";
import { trades } from "@/services/trades.service";

export const services = {
  trades,
  orders,
  blockers,
};
